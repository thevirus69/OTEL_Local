apiVersion: 1

providers:
  # This tells Grafana where to look for dashboards locally
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      # This matches the volume mount path in docker-compose.yml
      path: /var/lib/grafana/dashboards
